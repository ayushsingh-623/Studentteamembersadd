"""# Student Team Members Management Application - Todo List

## Phase 1: Project Setup & Backend

- [X] 1. Create root project directory (`student-team-app`).
- [X] 2. Create `backend` directory.
- [X] 3. Create `frontend` directory.
- [X] 4. Create root `README.md`.
- [X] 5. Create root `.gitignore`.
- [X] 6. Initialize backend project (`npm init -y` in `backend/`).
- [X] 7. Install backend dependencies (`express`, `mongoose`, `multer`, `cors`, `dotenv`, `nodemon` as dev dependency).
- [X] 8. Create backend directory structure (`models`, `routes`, `controllers`, `uploads`).
- [X] 9. Create backend `.gitignore`.
- [X] 10. Set up basic Express server (`backend/server.js`).
- [ ] 11. Configure MongoDB connection.
- [X] 12. Create Member MongoDB model (`backend/models/Member.js`).
- [X] 13. Implement API endpoint: `POST /api/members` (Add member with image upload).
- [X] 14. Implement API endpoint: `GET /api/members` (Retrieve all members).
- [X] 15. Implement API endpoint: `GET /api/members/:id` (Retrieve single member).
- [X] 16. Implement API endpoint: `PUT /api/members/:id` (Update member).
- [X] 17. Implement API endpoint: `DELETE /api/members/:id` (Delete member).
- [X] 18. Implement basic error handling for API routes.
- [X] 19. Configure static file serving for `uploads` directory.

## Phase 2: Frontend Development

- [X] 20. Initialize frontend project (`npx create-react-app frontend`).
- [X] 21. Install frontend dependencies (`axios`, `react-router-dom`).
- [X] 22. Create frontend directory structure (`components`, `pages`, `services`).
- [X] 23. Create frontend `.gitignore`.
- [X] 24. Set up React Router (`App.js`).
- [X] 25. Create Home Page component (`pages/HomePage.js`).
- [X] 26. Create Add Member Page component (`pages/AddMemberPage.js`).
- [X] 27. Create View Members Page component (`pages/ViewMembersPage.js`).
- [X] 28. Create Member Details Page component (`pages/MemberDetailsPage.js`).
- [X] 29. Create Edit Member Page component (`pages/EditMemberPage.js`).
- [X] 30. Create reusable Member Card component (`components/MemberCard.js`).
- [X] 31. Create Navigation component (`components/Navbar.js`).
- [X] 32. Implement Axios service for API calls (`services/api.js`).
- [X] 33. Implement "Add Member" form with validation and image upload.
- [X] 34. Implement "View Members" list/grid display.
- [X] 35. Implement "Member Details" display.
- [X] 36. Implement "Edit Member" form functionality.
- [X] 37. Implement "Delete Member" button functionality with confirmation.
- [X] 38. Apply simple, minimalistic, dark theme using basic CSS (`index.css` or separate CSS files).

## Phase 3: Integration, Testing & Documentation

- [ ] 39. Ensure frontend-backend communication works correctly.
- [ ] 40. Test all functionalities (Add, View, Details, Edit, Delete).
- [ ] 41. Test form validations.
- [ ] 42. Test image uploads and display.
- [ ] 43. Test responsiveness (basic).
- [ ] 44. Update root `README.md` with project description, installation, API endpoints, and run instructions.
- [ ] 45. Ensure `.gitignore` files are correctly configured.

## Phase 4: Delivery

- [ ] 46. Final review of code and structure.
- [ ] 47. Prepare project files for delivery (e.g., zip archive).
- [ ] 48. Notify user with the final project.
"""
